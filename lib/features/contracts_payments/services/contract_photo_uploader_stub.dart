import 'package:image_picker/image_picker.dart';

Future<String> uploadContractPhotoPlatform(XFile file) {
  throw UnsupportedError(
    'La subida de fotos no está disponible en esta plataforma.',
  );
}
